services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: releases
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: releases
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: